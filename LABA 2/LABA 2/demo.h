#include <iostream>
#include <vector>
#include <string>

std::vector<int> values;
std::vector<char> names;

int demo(std::vector<int> v1, std::vector<char> v2) {
	v1 = { 100, 40, 60, 70, 95, 95, 86, 77, 56, 51 };
	v2 = { 'P', 'K', 'N', 'M', 'T', 'L', 'O', 'G', 'A' };
}